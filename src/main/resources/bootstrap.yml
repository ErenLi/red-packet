spring:
  profiles:
    active: dev
  application:
    name: red-packet
  cloud:
    config:
      enabled: false
management:
  security:
    enabled: false
  context-path: /manage
logging:
  file: /Users/ln/logs/app.log